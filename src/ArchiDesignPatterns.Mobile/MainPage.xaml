<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="ArchiDesignPatterns.Mobile.MainPage"
             Shell.NavBarIsVisible="True"
             Title="Welcome">

    <ScrollView>
        <VerticalStackLayout Spacing="0">
            
            <!-- Hero Section -->
            <Border Background="{StaticResource PrimaryGradient}"
                    Padding="32,48,32,32"
                    StrokeThickness="0">
                <VerticalStackLayout Spacing="16" HorizontalOptions="Center">
                    <Image Source="dotnet_bot.png"
                           HeightRequest="120"
                           Aspect="AspectFit"
                           SemanticProperties.Description="Design Patterns Learning App" />
                    
                    <Label Text="Design Patterns"
                           FontSize="36"
                           FontAttributes="Bold"
                           TextColor="White"
                           HorizontalTextAlignment="Center" />
                    
                    <Label Text="Master Software Architecture &amp; Patterns"
                           FontSize="18"
                           TextColor="White"
                           Opacity="0.9"
                           HorizontalTextAlignment="Center"
                           SemanticProperties.HeadingLevel="Level2" />
                </VerticalStackLayout>
            </Border>

            <!-- Main Content -->
            <VerticalStackLayout Padding="20" Spacing="24">
                
                <!-- Welcome Message -->
                <VerticalStackLayout Spacing="8">
                    <Label Text="Welcome! 👋"
                           Style="{StaticResource Title}" />
                    <Label Text="Explore comprehensive design patterns and architectural styles used in modern software development. Learn through interactive examples and practical code demonstrations."
                           Style="{StaticResource BodyLarge}"
                           LineBreakMode="WordWrap" />
                </VerticalStackLayout>

                <!-- Quick Access Navigation Cards -->
                <VerticalStackLayout Spacing="16">
                    <Label Text="Quick Access"
                           Style="{StaticResource SubTitle}" />
                    
                    <!-- Design Patterns Card -->
                    <Border Style="{StaticResource ElevatedCard}">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnDesignPatternsClicked" />
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16" Padding="20">
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource Primary}"
                                    WidthRequest="60" HeightRequest="60"
                                    StrokeThickness="0"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Label Text="📐" FontSize="32"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                <Label Text="Design Patterns"
                                       Style="{StaticResource SubTitle}" />
                                <Label Text="23 proven software design solutions"
                                       Style="{StaticResource BodySmall}"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                            </VerticalStackLayout>
                            
                            <Label Grid.Column="2" Text="›" FontSize="40"
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center" />
                        </Grid>
                    </Border>

                    <!-- Architectures Card -->
                    <Border Style="{StaticResource ElevatedCard}">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnArchitecturesClicked" />
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16" Padding="20">
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource ArchitecturePattern}"
                                    WidthRequest="60" HeightRequest="60"
                                    StrokeThickness="0"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Label Text="🏛️" FontSize="32"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                <Label Text="Software Architectures"
                                       Style="{StaticResource SubTitle}" />
                                <Label Text="10 architectural patterns for scalable systems"
                                       Style="{StaticResource BodySmall}"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                            </VerticalStackLayout>
                            
                            <Label Grid.Column="2" Text="›" FontSize="40"
                                   TextColor="{StaticResource ArchitecturePattern}"
                                   VerticalOptions="Center" />
                        </Grid>
                    </Border>

                    <!-- Quiz Card -->
                    <Border Style="{StaticResource ElevatedCard}">
                        <Border.GestureRecognizers>
                            <TapGestureRecognizer Tapped="OnQuizClicked" />
                        </Border.GestureRecognizers>
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="16" Padding="20">
                            <Border Grid.Column="0"
                                    BackgroundColor="{StaticResource Tertiary}"
                                    WidthRequest="60" HeightRequest="60"
                                    StrokeThickness="0"
                                    VerticalOptions="Center">
                                <Border.StrokeShape>
                                    <RoundRectangle CornerRadius="12" />
                                </Border.StrokeShape>
                                <Label Text="🎯" FontSize="32"
                                       HorizontalOptions="Center" VerticalOptions="Center" />
                            </Border>
                            
                            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="4">
                                <Label Text="Test Your Knowledge"
                                       Style="{StaticResource SubTitle}" />
                                <Label Text="Interactive quizzes and challenges"
                                       Style="{StaticResource BodySmall}"
                                       TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                            </VerticalStackLayout>
                            
                            <Label Grid.Column="2" Text="›" FontSize="40"
                                   TextColor="{StaticResource Tertiary}"
                                   VerticalOptions="Center" />
                        </Grid>
                    </Border>
                </VerticalStackLayout>

                <!-- Feature Cards Grid -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12" RowSpacing="12">
                    
                    <!-- Patterns Card -->
                    <Border Grid.Column="0" Style="{StaticResource Card}">
                        <VerticalStackLayout Spacing="8" Padding="8">
                            <Label Text="📐" FontSize="40" HorizontalOptions="Center" />
                            <Label Text="23" 
                                   FontSize="32" FontAttributes="Bold"
                                   TextColor="{StaticResource Primary}"
                                   HorizontalTextAlignment="Center" />
                            <Label Text="Design Patterns"
                                   FontSize="12" FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                        </VerticalStackLayout>
                    </Border>

                    <!-- Architectures Card -->
                    <Border Grid.Column="1" Style="{StaticResource Card}">
                        <VerticalStackLayout Spacing="8" Padding="8">
                            <Label Text="🏛️" FontSize="40" HorizontalOptions="Center" />
                            <Label Text="10" 
                                   FontSize="32" FontAttributes="Bold"
                                   TextColor="{StaticResource ArchitecturePattern}"
                                   HorizontalTextAlignment="Center" />
                            <Label Text="Architectures"
                                   FontSize="12" FontAttributes="Bold"
                                   HorizontalTextAlignment="Center"
                                   TextColor="{AppThemeBinding Light={StaticResource Gray600}, Dark={StaticResource Gray400}}" />
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <!-- What You'll Learn Section -->
                <VerticalStackLayout Spacing="12">
                    <Label Text="What You'll Learn"
                           Style="{StaticResource SubTitle}" />
                    
                    <!-- Learning Item 1 -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border BackgroundColor="{StaticResource CreationalPattern}"
                                WidthRequest="32" HeightRequest="32"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 16">
                            <Label Text="✓" FontSize="18" TextColor="White"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Creational Patterns"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyMedium}" />
                            <Label Text="Object creation mechanisms"
                                   Style="{StaticResource Caption}" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Learning Item 2 -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border BackgroundColor="{StaticResource StructuralPattern}"
                                WidthRequest="32" HeightRequest="32"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 16">
                            <Label Text="✓" FontSize="18" TextColor="White"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Structural Patterns"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyMedium}" />
                            <Label Text="Class and object composition"
                                   Style="{StaticResource Caption}" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Learning Item 3 -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border BackgroundColor="{StaticResource BehavioralPattern}"
                                WidthRequest="32" HeightRequest="32"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 16">
                            <Label Text="✓" FontSize="18" TextColor="White"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Behavioral Patterns"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyMedium}" />
                            <Label Text="Object collaboration &amp; responsibilities"
                                   Style="{StaticResource Caption}" />
                        </VerticalStackLayout>
                    </Grid>

                    <!-- Learning Item 4 -->
                    <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                        <Border BackgroundColor="{StaticResource ArchitecturePattern}"
                                WidthRequest="32" HeightRequest="32"
                                StrokeThickness="0"
                                StrokeShape="RoundRectangle 16">
                            <Label Text="✓" FontSize="18" TextColor="White"
                                   HorizontalOptions="Center" VerticalOptions="Center" />
                        </Border>
                        <VerticalStackLayout Grid.Column="1" Spacing="2">
                            <Label Text="Software Architectures"
                                   FontAttributes="Bold"
                                   Style="{StaticResource BodyMedium}" />
                            <Label Text="Large-scale system organization"
                                   Style="{StaticResource Caption}" />
                        </VerticalStackLayout>
                    </Grid>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
