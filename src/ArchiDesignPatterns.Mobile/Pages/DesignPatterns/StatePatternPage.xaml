<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ArchiDesignPatterns.Mobile.ViewModels"
             x:Class="ArchiDesignPatterns.Mobile.Pages.DesignPatterns.StatePatternPage"
             x:DataType="viewmodels:StatePatternViewModel"
             Title="State">
    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Border Grid.Row="0"
                BackgroundColor="{StaticResource BehavioralPattern}"
                Padding="20,24,20,20"
                StrokeThickness="0">
            <VerticalStackLayout Spacing="8">
                <HorizontalStackLayout Spacing="12">
                    <Label Text="🔄" FontSize="32" VerticalOptions="Center" />
                    <VerticalStackLayout Spacing="4">
                        <Label Text="State Pattern"
                               FontSize="28" FontAttributes="Bold"
                               TextColor="White" />
                        <Border BackgroundColor="White" Opacity="0.3"
                                Padding="8,4" StrokeThickness="0"
                                StrokeShape="RoundRectangle 6"
                                HorizontalOptions="Start">
                            <Label Text="Comportemental" FontSize="11"
                                   FontAttributes="Bold" TextColor="White" />
                        </Border>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Border>
        <!-- Main Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="20" Spacing="15">
                <Label Text="State" FontSize="24" FontAttributes="Bold" />
                <Label Text="Le pattern State permet à un objet de changer son comportement lorsque son état change." FontSize="16" />
                <Border Stroke="Black" StrokeThickness="2" Padding="10" Margin="5">
                    <VerticalStackLayout>
                        <Label Text="Exemple interactif :" FontAttributes="Bold" />
                        <Button Text="Changer d'état" Clicked="OnChangeStateClicked" />
                        <Label x:Name="ResultLabel" FontSize="14" />
                    </VerticalStackLayout>
                </Border>
                <BoxView HeightRequest="1" BackgroundColor="Gray" Margin="0,10" />
                <Label Text="Exemple de code :" FontAttributes="Bold" />
                <Editor IsReadOnly="True" HeightRequest="120"
                    Text="// State interface&#x0a;public interface IState&#x0a;{&#x0a;    string Handle();&#x0a;}&#x0a;&#x0a;// Concrete States&#x0a;public class StateA : IState&#x0a;{&#x0a;    public string Handle() =&gt; &quot;A&quot;;&#x0a;}&#x0a;public class StateB : IState&#x0a;{&#x0a;    public string Handle() =&gt; &quot;B&quot;;&#x0a;}&#x0a;&#x0a;// Context&#x0a;public class Context&#x0a;{&#x0a;    public IState State;&#x0a;    public string Request() =&gt; State.Handle();&#x0a;}" />
            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>