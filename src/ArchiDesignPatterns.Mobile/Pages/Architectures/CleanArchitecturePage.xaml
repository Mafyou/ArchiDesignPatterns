<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ArchiDesignPatterns.Mobile.ViewModels"
             xmlns:resx="clr-namespace:ArchiDesignPatterns.Mobile.Resources"
             x:Class="ArchiDesignPatterns.Mobile.Pages.Architectures.CleanArchitecturePage"
             Title="{x:Static resx:Strings.CleanArchitecture_Title}">
    <ContentPage.BindingContext>
        <viewmodels:CleanArchitectureViewModel />
    </ContentPage.BindingContext>
    
    <Grid RowDefinitions="Auto,*">
        
        <Border Grid.Row="0"
                BackgroundColor="{StaticResource ArchitecturePattern}"
                Padding="20,24,20,20"
                StrokeThickness="0">
            <HorizontalStackLayout Spacing="12">
                <Label Text="🏛️" FontSize="32" VerticalOptions="Center" />
                <VerticalStackLayout Spacing="4">
                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Title}"
                           FontSize="28" FontAttributes="Bold"
                           TextColor="White" />
                    <Border BackgroundColor="White" Opacity="0.3"
                            Padding="8,4" StrokeThickness="0"
                            StrokeShape="RoundRectangle 6"
                            HorizontalOptions="Start">
                        <Label Text="{x:Static resx:Strings.CleanArchitecture_Architecture}" FontSize="11"
                               FontAttributes="Bold" TextColor="White" />
                    </Border>
                </VerticalStackLayout>
            </HorizontalStackLayout>
        </Border>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Style="{StaticResource PatternDetailContainer}">
                
                <Border Style="{StaticResource ElevatedCard}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="📖" FontSize="20" />
                            <Label Text="{x:Static resx:Strings.SingletonPattern_DescriptionTitle}"
                               Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="{x:Static resx:Strings.CleanArchitecture_Description}"
                               Style="{StaticResource BodyLarge}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="🎯" FontSize="20" />
                            <Label Text="{x:Static resx:Strings.CleanArchitecture_PrinciplesTitle}"
                               Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="1" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle1Title}"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle1Desc}"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                            
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="2" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle2Title}"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle2Desc}"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                            
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="3" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle3Title}"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="{x:Static resx:Strings.CleanArchitecture_Principle3Desc}"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="🔄" FontSize="20" />
                            <Label Text="{x:Static resx:Strings.CleanArchitecture_LayersTitle}"
                               Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        
                        <Border BackgroundColor="{StaticResource Primary}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer1}" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer1Desc}"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Info}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer2}" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer2Desc}"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Warning}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer3}" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer3Desc}"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Success}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer4}" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Layer4Desc}"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource InteractiveSection}">
                    <VerticalStackLayout Spacing="16">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="🎮" FontSize="20" VerticalOptions="Center" />
                            <Label Text="{x:Static resx:Strings.SingletonPattern_InteractiveTitle}"
                                   Style="{StaticResource SubTitle}"
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="{x:Static resx:Strings.CleanArchitecture_InteractiveDesc}"
                               Style="{StaticResource BodyMedium}"
                               LineBreakMode="WordWrap" />
                        <Button Text="{x:Static resx:Strings.CleanArchitecture_InteractiveButton}"
                                Clicked="OnSimulateServiceClicked"
                                HorizontalOptions="Fill" />
                        <Label x:Name="ResultLabel"
                               Style="{StaticResource ResultLabel}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource CodeSection}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="💻" FontSize="20" />
                            <Label Text="{x:Static resx:Strings.CleanArchitecture_CodeTitle}"
                                   Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Editor Style="{StaticResource CodeEditor}"
                                Text="// Core Layer - Business Logic&#x0a;public interface IUserRepository&#x0a;{&#x0a;    User GetById(int id);&#x0a;}&#x0a;&#x0a;// Application Layer - Use Case&#x0a;public class GetUserUseCase&#x0a;{&#x0a;    private readonly IUserRepository _repo;&#x0a;    &#x0a;    public GetUserUseCase(IUserRepository repo)&#x0a;        => _repo = repo;&#x0a;    &#x0a;    public User Execute(int id)&#x0a;        => _repo.GetById(id);&#x0a;}&#x0a;&#x0a;// Infrastructure - Implementation&#x0a;public class UserRepository : IUserRepository&#x0a;{&#x0a;    public User GetById(int id)&#x0a;        => // Database access&#x0a;}&#x0a;&#x0a;// Presentation - Controller&#x0a;public class UserController&#x0a;{&#x0a;    private readonly GetUserUseCase _useCase;&#x0a;    &#x0a;    public User Get(int id)&#x0a;        => _useCase.Execute(id);&#x0a;}" />
                    </VerticalStackLayout>
                </Border>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Border Style="{StaticResource ProCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="✅" FontSize="20" />
                                <Label Text="{x:Static resx:Strings.SingletonPattern_ProsTitle}" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Pros1}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Pros2}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Pros3}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Pros4}"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                    
                    <Border Style="{StaticResource ConCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="⚠️" FontSize="20" />
                                <Label Text="{x:Static resx:Strings.Architecture_ConsTitle}" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Cons1}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Cons2}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Cons3}"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="{x:Static resx:Strings.CleanArchitecture_Cons4}"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <Border BackgroundColor="{AppThemeBinding Light=#EFF6FF, Dark=#1E3A8A}"
                        Stroke="{StaticResource Info}"
                        StrokeThickness="2"
                        Padding="20"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="💡" FontSize="24" />
                            <Label Text="{x:Static resx:Strings.Architecture_KeyPointTitle}"
                                   FontAttributes="Bold"
                                   Style="{StaticResource Title}" />
                        </HorizontalStackLayout>
                        <Label Text="{x:Static resx:Strings.CleanArchitecture_KeyPoint}"
                               Style="{StaticResource BodyMedium}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
