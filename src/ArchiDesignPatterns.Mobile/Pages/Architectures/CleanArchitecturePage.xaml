<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ArchiDesignPatterns.Mobile.ViewModels"
             x:Class="ArchiDesignPatterns.Mobile.Pages.Architectures.CleanArchitecturePage"
             Title="Clean Architecture">
    <ContentPage.BindingContext>
        <viewmodels:CleanArchitectureViewModel />
    </ContentPage.BindingContext>
    
    <Grid RowDefinitions="Auto,*">
        
        <Border Grid.Row="0"
                BackgroundColor="{StaticResource ArchitecturePattern}"
                Padding="20,24,20,20"
                StrokeThickness="0">
            <HorizontalStackLayout Spacing="12">
                <Label Text="🏛️" FontSize="32" VerticalOptions="Center" />
                <VerticalStackLayout Spacing="4">
                    <Label Text="Clean Architecture"
                           FontSize="28" FontAttributes="Bold"
                           TextColor="White" />
                    <Border BackgroundColor="White" Opacity="0.3"
                            Padding="8,4" StrokeThickness="0"
                            StrokeShape="RoundRectangle 6"
                            HorizontalOptions="Start">
                        <Label Text="Architecture" FontSize="11"
                               FontAttributes="Bold" TextColor="White" />
                    </Border>
                </VerticalStackLayout>
            </HorizontalStackLayout>
        </Border>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Style="{StaticResource PatternDetailContainer}">
                
                <Border Style="{StaticResource ElevatedCard}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="📖 Description"
                               Style="{StaticResource SubTitle}" />
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="Clean Architecture organise le code en couches indépendantes avec des frontières claires et des dépendances pointant vers l'intérieur. Les couches internes contiennent la logique métier et sont indépendantes des frameworks, de l'UI et des bases de données."
                               Style="{StaticResource BodyLarge}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="🎯 Principes Fondamentaux"
                               Style="{StaticResource SubTitle}" />
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <VerticalStackLayout Spacing="10">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="1" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="Indépendance"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="Logique métier indépendante des frameworks"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                            
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="2" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="Testabilité"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="Facile à tester sans UI ou base de données"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                            
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Border BackgroundColor="{StaticResource Primary}"
                                        WidthRequest="32" HeightRequest="32"
                                        StrokeThickness="0"
                                        StrokeShape="RoundRectangle 16">
                                    <Label Text="3" FontSize="16" TextColor="White" FontAttributes="Bold"
                                           HorizontalOptions="Center" VerticalOptions="Center" />
                                </Border>
                                <VerticalStackLayout Grid.Column="1" Spacing="4">
                                    <Label Text="Règle de Dépendance"
                                           FontAttributes="Bold"
                                           Style="{StaticResource BodyMedium}" />
                                    <Label Text="Dépendances pointent seulement vers l'intérieur"
                                           Style="{StaticResource BodySmall}"
                                           TextColor="{StaticResource Gray600}" />
                                </VerticalStackLayout>
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="🔄 Couches Architecturales"
                               Style="{StaticResource SubTitle}" />
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        
                        <Border BackgroundColor="{StaticResource Primary}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="Core" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="→ Entités, Règles Métier"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Info}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="Application" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="→ Cas d'usage, Interfaces"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Warning}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="Infrastructure" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="→ Base de données, Services externes"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                        
                        <Border BackgroundColor="{StaticResource Success}"
                                Padding="16,12" StrokeThickness="0"
                                StrokeShape="RoundRectangle 12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="Présentation" TextColor="White"
                                       FontAttributes="Bold" FontSize="14" />
                                <Label Text="→ UI, Contrôleurs, ViewModels"
                                       TextColor="White" FontSize="13" />
                            </HorizontalStackLayout>
                        </Border>
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource InteractiveSection}">
                    <VerticalStackLayout Spacing="16">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="🎮" FontSize="20" VerticalOptions="Center" />
                            <Label Text="Exemple Interactif"
                                   Style="{StaticResource SubTitle}"
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="Simulez un appel de service à travers les couches :"
                               Style="{StaticResource BodyMedium}"
                               LineBreakMode="WordWrap" />
                        <Button Text="Exécuter l'Appel de Service"
                                Clicked="OnSimulateServiceClicked"
                                HorizontalOptions="Fill" />
                        <Label x:Name="ResultLabel"
                               Style="{StaticResource ResultLabel}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <Border Style="{StaticResource CodeSection}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="💻" FontSize="20" />
                            <Label Text="Structure du Code"
                                   Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Editor Style="{StaticResource CodeEditor}"
                                Text="// Core Layer - Business Logic&#x0a;public interface IUserRepository&#x0a;{&#x0a;    User GetById(int id);&#x0a;}&#x0a;&#x0a;// Application Layer - Use Case&#x0a;public class GetUserUseCase&#x0a;{&#x0a;    private readonly IUserRepository _repo;&#x0a;    &#x0a;    public GetUserUseCase(IUserRepository repo)&#x0a;        => _repo = repo;&#x0a;    &#x0a;    public User Execute(int id)&#x0a;        => _repo.GetById(id);&#x0a;}&#x0a;&#x0a;// Infrastructure - Implementation&#x0a;public class UserRepository : IUserRepository&#x0a;{&#x0a;    public User GetById(int id)&#x0a;        => // Database access&#x0a;}&#x0a;&#x0a;// Presentation - Controller&#x0a;public class UserController&#x0a;{&#x0a;    private readonly GetUserUseCase _useCase;&#x0a;    &#x0a;    public User Get(int id)&#x0a;        => _useCase.Execute(id);&#x0a;}" />
                    </VerticalStackLayout>
                </Border>

                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <Border Style="{StaticResource ProCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="✅" FontSize="20" />
                                <Label Text="Avantages" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="• Hautement testable"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Indépendant des technologies"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Frontières claires"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Facile à maintenir"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                    
                    <Border Style="{StaticResource ConCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="⚠️" FontSize="20" />
                                <Label Text="Défis" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="• Courbe d'apprentissage"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Plus de code standard"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Excessif pour petites apps"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Complexité initiale"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                </Grid>

                <Border BackgroundColor="{AppThemeBinding Light=#EFF6FF, Dark=#1E3A8A}"
                        Stroke="{StaticResource Info}"
                        StrokeThickness="2"
                        Padding="20"
                        StrokeShape="RoundRectangle 12">
                    <VerticalStackLayout Spacing="8">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="💡" FontSize="24" />
                            <Label Text="Point Clé"
                                   FontAttributes="Bold"
                                   Style="{StaticResource Title}" />
                        </HorizontalStackLayout>
                        <Label Text="Clean Architecture privilégie la séparation des préoccupations et l'indépendance des dépendances externes, rendant votre base de code plus maintenable, testable et adaptable aux changements."
                               Style="{StaticResource BodyMedium}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
