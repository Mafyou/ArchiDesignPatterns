<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:ArchiDesignPatterns.Mobile.ViewModels"
             x:Class="ArchiDesignPatterns.Mobile.Pages.Architectures.MicroservicesArchitecturePage"
             Title="Microservices Architecture">
    <ContentPage.BindingContext>
        <viewmodels:MicroservicesArchitectureViewModel />
    </ContentPage.BindingContext>
    <Grid RowDefinitions="Auto,*">
        <!-- Header -->
        <Border Grid.Row="0"
                BackgroundColor="{StaticResource ArchitecturePattern}"
                Padding="20,24,20,20"
                StrokeThickness="0">
            <VerticalStackLayout Spacing="8">
                <HorizontalStackLayout Spacing="12">
                    <Label Text="🔧" FontSize="32" VerticalOptions="Center" />
                    <VerticalStackLayout Spacing="4">
                        <Label Text="Microservices Architecture"
                               FontSize="28" FontAttributes="Bold"
                               TextColor="White" />
                        <Border BackgroundColor="White" Opacity="0.3"
                                Padding="8,4" StrokeThickness="0"
                                StrokeShape="RoundRectangle 6"
                                HorizontalOptions="Start">
                            <Label Text="Architecture" FontSize="11"
                                   FontAttributes="Bold" TextColor="White" />
                        </Border>
                    </VerticalStackLayout>
                </HorizontalStackLayout>
            </VerticalStackLayout>
        </Border>
        <!-- Content -->
        <ScrollView Grid.Row="1">
            <VerticalStackLayout Style="{StaticResource PatternDetailContainer}">
                
                <!-- Description Card -->
                <Border Style="{StaticResource ElevatedCard}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="📖 Description"
                               Style="{StaticResource SubTitle}" />
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="L'architecture Microservices décompose l'application en services indépendants, chacun responsable d'une fonctionnalité métier spécifique, déployable et scalable séparément."
                               Style="{StaticResource BodyLarge}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <!-- When to Use -->
                <Border Style="{StaticResource Card}">
                    <VerticalStackLayout Spacing="12">
                        <Label Text="🎯 Quand l'utiliser"
                               Style="{StaticResource SubTitle}" />
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <VerticalStackLayout Spacing="8">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Label Text="✓" FontSize="18" TextColor="{StaticResource Success}" />
                                <Label Grid.Column="1" Text="Pour des applications complexes nécessitant scalabilité"
                                       Style="{StaticResource BodyMedium}" LineBreakMode="WordWrap" />
                            </Grid>
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Label Text="✓" FontSize="18" TextColor="{StaticResource Success}" />
                                <Label Grid.Column="1" Text="Quand plusieurs équipes travaillent en parallèle"
                                       Style="{StaticResource BodyMedium}" LineBreakMode="WordWrap" />
                            </Grid>
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                                <Label Text="✓" FontSize="18" TextColor="{StaticResource Success}" />
                                <Label Grid.Column="1" Text="Pour des déploiements indépendants et fréquents"
                                       Style="{StaticResource BodyMedium}" LineBreakMode="WordWrap" />
                            </Grid>
                        </VerticalStackLayout>
                    </VerticalStackLayout>
                </Border>

                <!-- Interactive Example -->
                <Border Style="{StaticResource InteractiveSection}">
                    <VerticalStackLayout Spacing="16">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="🎮" FontSize="20" VerticalOptions="Center" />
                            <Label Text="Exemple interactif"
                                   Style="{StaticResource SubTitle}"
                                   TextColor="{StaticResource Primary}"
                                   VerticalOptions="Center" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Label Text="Appelez un microservice :"
                               Style="{StaticResource BodyMedium}" />
                        <Button Text="Appeler un microservice"
                                Clicked="OnCallMicroserviceClicked"
                                HorizontalOptions="Fill" />
                        <Label x:Name="ResultLabel"
                               Style="{StaticResource ResultLabel}"
                               LineBreakMode="WordWrap" />
                    </VerticalStackLayout>
                </Border>

                <!-- Code Example -->
                <Border Style="{StaticResource CodeSection}">
                    <VerticalStackLayout Spacing="12">
                        <HorizontalStackLayout Spacing="8">
                            <Label Text="💻" FontSize="20" />
                            <Label Text="Exemple de structure"
                                   Style="{StaticResource SubTitle}" />
                        </HorizontalStackLayout>
                        <BoxView Style="{StaticResource Divider}" Margin="0,0" />
                        <Editor Style="{StaticResource CodeEditor}"
                                Text="// Interface commune&#x0a;public interface IMicroservice&#x0a;{&#x0a;    string Execute();&#x0a;}&#x0a;&#x0a;// Service de commandes&#x0a;public class OrderService : IMicroservice&#x0a;{&#x0a;    public string Execute()&#x0a;    {&#x0a;        // Logique de traitement des commandes&#x0a;        return &quot;Commande créée et traitée&quot;;&#x0a;    }&#x0a;}&#x0a;&#x0a;// Service utilisateurs&#x0a;public class UserService : IMicroservice&#x0a;{&#x0a;    public string Execute()&#x0a;    {&#x0a;        // Logique de gestion des utilisateurs&#x0a;        return &quot;Utilisateur créé avec succès&quot;;&#x0a;    }&#x0a;}&#x0a;&#x0a;// Service de paiement&#x0a;public class PaymentService : IMicroservice&#x0a;{&#x0a;    public string Execute()&#x0a;    {&#x0a;        // Logique de traitement des paiements&#x0a;        return &quot;Paiement traité&quot;;&#x0a;    }&#x0a;}&#x0a;&#x0a;// API Gateway&#x0a;public class ApiGateway&#x0a;{&#x0a;    public string RouteRequest(string service)&#x0a;    {&#x0a;        IMicroservice microservice = service switch&#x0a;        {&#x0a;            &quot;order&quot; =&gt; new OrderService(),&#x0a;            &quot;user&quot; =&gt; new UserService(),&#x0a;            &quot;payment&quot; =&gt; new PaymentService(),&#x0a;            _ =&gt; throw new Exception(&quot;Service introuvable&quot;)&#x0a;        };&#x0a;        return microservice.Execute();&#x0a;    }&#x0a;}" />
                    </VerticalStackLayout>
                </Border>

                <!-- Pros and Cons -->
                <Grid ColumnDefinitions="*,*" ColumnSpacing="12">
                    <!-- Pros -->
                    <Border Style="{StaticResource ProCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="✅" FontSize="20" />
                                <Label Text="Avantages" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="• Scalabilité indépendante"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Déploiement continu"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Technologie flexible"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Équipes autonomes"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                    
                    <!-- Cons -->
                    <Border Style="{StaticResource ConCard}">
                        <VerticalStackLayout Spacing="12">
                            <HorizontalStackLayout Spacing="8">
                                <Label Text="⚠️" FontSize="20" />
                                <Label Text="Inconvénients" FontAttributes="Bold"
                                       Style="{StaticResource BodyLarge}" />
                            </HorizontalStackLayout>
                            <VerticalStackLayout Spacing="6">
                                <Label Text="• Complexité opérationnelle"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Communication réseau"
                                       Style="{StaticResource BodySmall}" />
                                <Label Text="• Cohérence des données"
                                       Style="{StaticResource BodySmall}" />
                            </VerticalStackLayout>
                        </VerticalStackLayout>
                    </Border>
                </Grid>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>
